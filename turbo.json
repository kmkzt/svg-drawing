{
  "$schema": "https://turborepo.org/schema.json",
  "baseBranch": "origin/main",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["lib/**", "./next/**"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["snapshots/**"]
    },
    "dev": {
      "cache": false
    }
  }
}
